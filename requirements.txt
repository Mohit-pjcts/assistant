anthropic>=0.40.0
langchain>=1.0.0
langchain-anthropic>=0.3.0
langchain-tavily>=0.1.0
langchain-mcp-adapters==0.3.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
aiosqlite>=0.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
sounddevice>=0.5.0
numpy>=1.26.0
pynput>=1.8.0
rumps>=0.4.0; sys_platform == "darwin"

# Phase 9: assistant/server.py, the dashboard app's backend wrapper
fastapi>=0.115.0
uvicorn>=0.30.0
langsmith>=0.10.0

# Phase 16 Part B: Langfuse v3 tracing (additive to LangSmith, not a
# replacement). Pinned <4.0 deliberately -- this migration's target is v3,
# not v4 (which ships a differently-shaped span-filter API confirmed NOT
# present here, STEPS.md 87); see assistant/observability.py's docstring
# for the full v2->v3 migration story.
langfuse>=3.0,<4.0

# Phase 16 Part A.5: the mentor's distributed-tracing spike. Was already a
# transitive dep (via anthropic's own SDK); made explicit now that
# supervisor.py/fa_service.py import it directly for the FA proxy's HTTP
# calls (a real code-review finding -- an undeclared direct import).
httpx>=0.27.0

# Phase 16 Part B: OTEL trace-context propagation across the FA process
# boundary (assistant/observability.py's inject_trace_headers()/
# attached_parent_context() import `opentelemetry.propagate`/`.context`
# directly). Was already a transitive dep of langfuse; made explicit now
# for the same reason httpx is above -- a real /code-review max finding
# (STEPS.md 91) caught this as an undeclared direct import.
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0

# Dev-only: LangGraph Studio (`langgraph dev`), not needed to run `assistant`
langgraph-cli[inmem]>=0.4.0

# Dev-only: test runner (Phase 10) -- tests/ was already pytest-shaped
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Dev-only: lint/format (GitHub cleanup pass)
ruff>=0.8.0
